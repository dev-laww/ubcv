import { Center } from '@mantine/core';

export const metadata = {
    title: 'Chat'
}

const Chat = () => {
    return (
        <Center h='100vh'>
            <h1>Chat</h1>
        </Center>
    )
}